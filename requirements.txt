# Runtime dependencies for the FastAPI backend (api/) and the services/
# clients/common layers it wires together. Install with:
#   pip install -r requirements.txt

fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-dotenv>=1.0

# LLM calls (OpenAI-compatible client, local Ollama by default)
openai>=1.50

# clients/ecommerce_api_client.py: SQLite locally, Postgres via DATABASE_URL
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# services/classifier_client.py: classifier service HTTP client
requests>=2.31

# services/rag.py: FAISS vector index + embeddings
faiss-cpu>=1.8
numpy>=1.26

# services/data_pipeline.py: PDF ingestion and text chunking
pypdf>=4.0
reportlab>=4.0
langchain-text-splitters>=0.3

# common/langfuse_config.py: LLM call tracing (disabled if keys unset)
langfuse>=2.50,<5
